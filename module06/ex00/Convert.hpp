#ifndef CONVERT_HPP
# define CONVERT_HPP

# include <iostream>
# include <string>

class Convert
{
	public:
		// Constructors
		Convert();
		Convert(const Convert &copy);
		
		// Destructor
		~Convert();
		
		// Operators
		Convert & operator=(const Convert &assign);
		
		
	private:
		
};

#endif